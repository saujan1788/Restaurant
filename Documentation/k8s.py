def LackOfSpace:
    Pods were failing as no space on node , Added extra EBS volumes increased from 8 to 15, Used ansible to claim that on all nodes and pods ran fine

    For my python application better to run jobs instead of pods look into it
